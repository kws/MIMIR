# Media Fixtures

This directory holds prerecorded WAV fixtures used to verify the media bridge against live model runtimes.

Expected format:

- mono
- 16-bit PCM
- `.wav`

`hello-mimir.wav` can be generated locally with `scripts/generate_fixture.sh`, or replaced with any short speech sample that matches the same format.
